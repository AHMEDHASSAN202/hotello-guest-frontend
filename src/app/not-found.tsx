import { HotelNotFoundScreen } from '@/components/state-screens';

/** Unknown slug or stray URL — branded, guest-language, warm (14.1 AC5). */
export default function NotFound() {
  return <HotelNotFoundScreen />;
}
